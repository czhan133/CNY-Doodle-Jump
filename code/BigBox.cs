using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BigBox : MonoBehaviour
{
    //public Transform box;
    public GameObject platform_prefab;
    //private GameObject new_platform;

    /*void Start()
    {
        Vector3 spawn = new Vector3();

        for (int i = 0; i < 6; i++)
        {
            spawn.y += Random.Range(1f, 3f);
            spawn.x = Random.Range(-2.5f, 2.5f);

            //Instantiate(platform_prefab, spawn, Quaternion.identity);
        }

        //new_platform = (GameObject)Instantiate(platform_prefab, new Vector2(Random.Range(-2.6f, 2.5f), box.position.y + Random.Range(0.01f, 0.3f)), Quaternion.identity);

    }*/
}
